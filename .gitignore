
.venv/

.cursor/mcp.json

.ipynb_checkpoints/

# Python bytecode cache
__pycache__/
*.pyc

.DS_Store

*.code-workspace

# MkDocs build output (deployed by CI artifact, not committed)
site/

# Generated PDF copies under docs/ (sync_site_docs.py copies them from the
# committed source PDFs in papers/ at build time; avoid duplicating binaries)
docs/pdf/

# LaTeX intermediates (keep sources / intentional PDFs)
*.aux
*.fdb_latexmk
*.fls
*.log
*.toc
*.xdv
*.out
*.synctex.gz
